interface:
  display_name: "MySearch"
  short_description: "Install, debug, and use MySearch for web, docs, and X"
  default_prompt: "Use $mysearch to install or debug the MySearch skill/MCP, then search the web, docs, and X with the right provider automatically."

policy:
  allow_implicit_invocation: true
